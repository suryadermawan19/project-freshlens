name: freshlens_ai_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Packages (Updated to compatible versions)
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  google_sign_in: ^6.2.1
  cloud_firestore: ^5.0.2

  # UI & Utility Packages
  font_awesome_flutter: ^10.7.0
  camera: ^0.11.0+1
  flutter_slidable: ^3.1.0
  webview_flutter: ^4.8.2
  cupertino_icons: ^1.0.8
  firebase_storage: ^12.4.10

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Aset path diperbaiki (menggunakan '/' bukan '.')
  assets:
    - assets/images/